# Public showcase needs no environment variables.
# A private deployment must stay locked until deployment-owned auth, policy,
# storage, audit, export, and restore adapters are connected and verified.
ATLAS_INSTANCE_MODE=public-safe
